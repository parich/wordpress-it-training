# Local WP — ติดตั้งและใช้งาน
[← กลับหน้าหลัก](WordPress_Training_IT_2Days.md)

> **"Local WP คือโปรแกรมที่ทำให้รัน WordPress บนเครื่องได้ง่ายที่สุด — คลิกเดียว ไม่ต้องตั้งค่า server เอง"**

---

## 1. Local WP คืออะไร

- โปรแกรม GUI สำหรับ **Local WordPress Development**
- พัฒนาโดย **WP Engine** (บริษัท WordPress hosting ชั้นนำ)
- ใช้ **Docker** ทำงานอยู่เบื้องหลัง แต่ซ่อน complexity ทั้งหมด
- รองรับ Windows, macOS, Linux
- **ฟรี** สำหรับการใช้งานส่วนตัว

### เปรียบเทียบกับ XAMPP และ Docker

| หัวข้อ | XAMPP | Local WP | Docker |
|--------|-------|----------|--------|
| ง่ายแค่ไหน | ปานกลาง | ง่ายมาก | ยาก |
| GUI | ✅ | ✅ | ❌ (mostly CLI) |
| รองรับหลาย PHP version | ❌ | ✅ | ✅ |
| Isolation (แยกแต่ละเว็บ) | ❌ | ✅ | ✅ |
| ใกล้เคียง production | น้อย | ปานกลาง | มาก |
| เหมาะกับ | เรียนรู้ | dev WordPress | devops/pro |

---

## 2. โหลดและติดตั้ง

### ขั้นตอนที่ 1 — โหลด

1. ไปที่ **[localwp.com](https://localwp.com)**
2. คลิก **"Download"**
3. เลือก OS: **Windows** / macOS / Linux
4. กรอก email (หรือข้าม) → ดาวน์โหลด

### ขั้นตอนที่ 2 — ติดตั้ง (Windows)

1. เปิดไฟล์ `Local-x.x.x-windows.exe`
2. คลิก **Install** → รอสักครู่
3. โปรแกรมเปิดขึ้นมาอัตโนมัติ

> ระบบต้องการ: Windows 10/11, RAM 4GB+, พื้นที่ว่าง 2GB+

---

## 3. สร้าง WordPress Site แรก

### ขั้นตอน

**1. คลิก `+` หรือ "Create a new site"**

**2. ตั้งชื่อ site**
```
Site name: my-wordpress-site
```

**3. เลือก Environment**
- **Preferred** (แนะนำ) — Local เลือกให้อัตโนมัติ
- **Custom** — เลือก PHP version, Web server, MySQL version เอง

```
PHP Version:    8.2 (แนะนำ)
Web Server:     nginx
Database:       MySQL 8.0
```

**4. ตั้งค่า WordPress**
```
WordPress Username: admin
WordPress Password: admin
WordPress Email:    admin@example.com
```

**5. คลิก "Add Site"** → รอ 1-2 นาที

---

## 4. หน้าตา Dashboard

```
┌─────────────────────────────────────────────┐
│  my-wordpress-site              ● Running   │
│                                             │
│  Site Domain:  my-wordpress-site.local      │
│  SSL:          ✅ Trusted                   │
│                                             │
│  [ Open Site ]  [ WP Admin ]               │
│                                             │
│  PHP:    8.2    Web: nginx    DB: MySQL 8.0 │
│                                             │
│  [ Stop Site ]                              │
└─────────────────────────────────────────────┘
```

### ปุ่มหลัก

| ปุ่ม | ทำอะไร |
|------|--------|
| **Open Site** | เปิดหน้าเว็บ `https://my-wordpress-site.local` |
| **WP Admin** | เปิด `/wp-admin` login อัตโนมัติ |
| **Stop Site** | หยุด site นั้น (ไม่กระทบ site อื่น) |
| **Start Site** | เปิด site ที่หยุดไว้ |

---

## 5. ฟีเจอร์เด่น

### 5.1 Instant Reload (Live Link)

แชร์เว็บให้คนอื่นดูผ่านอินเทอร์เน็ตได้ชั่วคราว:

```
Live Links tab → Enable → ได้ URL เช่น
https://happy-tiger-123.localsite.io
```

> ใช้สำหรับโชว์ client หรือทดสอบบนมือถือ — ไม่ต้อง deploy จริง

### 5.2 SSL ในเครื่อง (https://)

Local สร้าง SSL certificate ให้อัตโนมัติ — browser แสดง 🔒 เลย ไม่ต้องตั้งค่าเอง

### 5.3 เปลี่ยน PHP Version ได้

```
Site → Config → PHP Version → เลือก 7.4 / 8.0 / 8.1 / 8.2
```

แต่ละ site ใช้ PHP version ต่างกันได้ — ต่างจาก XAMPP ที่ใช้ version เดียวทั้งหมด

### 5.4 Database Manager (Adminer)

```
Site → Database tab → Open Adminer
```

คล้าย phpMyAdmin — ดู/แก้ไขข้อมูลใน DB ได้เลย

### 5.5 SSH / Shell เข้า Container

```
Site → Open Site Shell
```

เปิด terminal เข้าไปใน container โดยตรง:

```bash
# ใช้ WP-CLI ได้เลย
wp plugin list
wp user list
wp cache flush
```

---

## 6. ไฟล์อยู่ที่ไหน

```
Windows:
C:\Users\{username}\Local Sites\{site-name}\

└── app\
    └── public\          ← WordPress files อยู่ที่นี่
        ├── wp-content\
        ├── wp-config.php
        └── index.php

└── conf\               ← config ของ nginx, php, mysql
└── logs\               ← log files
```

เปิดใน VSCode ได้เลย:
```
Site → Go to Site Folder → เปิด Explorer
```

---

## 7. Export / Import Site

### Export (สำรองข้อมูล)

```
Site → ... (menu) → Export → บันทึกเป็น .zip
```

ไฟล์ `.zip` มีทั้ง WordPress files + Database dump

### Import

```
+ (New Site) → Import Existing Site → เลือก .zip
```

ใช้แชร์งานระหว่างทีม หรือย้ายไปเครื่องอื่นได้

---

## 8. เชื่อมกับ Git

ไฟล์ WordPress อยู่ใน folder ปกติ → ใช้ git ได้เลย:

```bash
cd "C:/Users/username/Local Sites/my-site/app/public"
git init
git add wp-content/themes/my-theme
git commit -m "Initial theme"
```

---

## 9. สรุป Flow การใช้งาน

```
1. เปิด Local WP
2. Start site ที่ต้องการ
3. คลิก "WP Admin" → แก้ไขเนื้อหา
4. คลิก "Open Site" → ดูผลลัพธ์
5. แก้ไขไฟล์ใน VSCode (wp-content/themes/...)
6. ดูผลทันทีใน browser (Auto-refresh)
7. เสร็จแล้ว → Stop site
```

---

## 10. ปัญหาที่พบบ่อย

| ปัญหา | วิธีแก้ |
|-------|---------|
| Site เปิดไม่ได้ | ตรวจ Antivirus / Firewall บล็อก Local อยู่ไหม |
| `.local` domain ไม่ทำงาน | ตรวจ hosts file: `C:\Windows\System32\drivers\etc\hosts` |
| Port conflict | Local ใช้ port random — ไม่ค่อยชน แต่ถ้าชนให้ restart Local |
| Docker ไม่ start | Local ต้องการ Docker Desktop — ตรวจให้แน่ใจว่าเปิดอยู่ |
